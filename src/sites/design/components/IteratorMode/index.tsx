import React, {FC} from 'react';
import { Boss } from './Boss';

const IteratorMode: FC = () => {
    const boss: Boss = new Boss();
    return (
        <div>
            boss
        </div>
    );
};

export default IteratorMode;